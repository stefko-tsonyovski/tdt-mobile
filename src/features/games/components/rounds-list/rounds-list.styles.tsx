import { View } from "react-native";
import styled from "styled-components/native";

export const RoundsListContainer = styled(View)`
  flex-direction: row;
  flex-wrap: wrap;
  justify-content: center;
`;
