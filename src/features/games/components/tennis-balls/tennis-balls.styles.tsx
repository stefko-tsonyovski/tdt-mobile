import { View } from "react-native";
import styled from "styled-components/native";

export const TennisBallsContainer = styled(View)`
  flex-direction: row;
  justify-content: center;
  flex-wrap: wrap;
`;
